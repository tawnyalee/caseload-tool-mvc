# CLAUDE.md — Action Manager App

## Project overview
Python/CustomTkinter desktop app, rewritten from a previous non-professional dev's spaghetti-code app into a clean MVC design. The app helps professors automate routine student communications (emails, texts, notes) and document them, organized into "Groups" (school courses).

The developer working on this is a C# developer who is newer to Python — favor clear, idiomatic Python and explain any non-obvious language features when they come up.

## Architecture
- `src/models/` — Action, Group, Student, EmailTemplate, and enum definitions. `Student` is a `@dataclass` with 68 fields mirroring the real caseload export (course status, tasks 1-15, mentor info, SAP/CU figures, dates, etc.) on top of the original core fields (`salesforce_id`, `first_name`, `last_name`, `email`, `phone`, `has_signed_up_for_text`); new fields are kept as plain strings on purpose since type-aware filtering is still undesigned (see Filters below). `Student.SALESFORCE_FIELD_MAP` ties each field back to its exact source column name for the real Salesforce adapter to be built later.
- `src/services/` — `ActionRepository`, `GroupRepository`, `TemplateRepository` (JSON file storage); `StudentDataProvider`, `EmailSender`, `TextSender`, `NoteWriter` (abstract interfaces with local `Fake*` implementations — real Salesforce/Cadence/Outlook adapters not built yet, see Known open items); `OutlookSignatureProvider`/`LocalOutlookSignatureProvider` (the one real, non-fake integration so far — lists actual Outlook signature names from `%APPDATA%\Microsoft\Signatures\`, used by both the Action editor and the ad-hoc email modal; `FakeOutlookSignatureProvider` exists for tests); `ActionRunner` (orchestrates running an action, welcome emails, or an ad-hoc send against students, with per-student failure isolation and logging — see Design decisions); `ActivityLogger` (writes to a daily log file and the Live Application Log panel from one call site); `html_richtext` (HTML ⇄ `tkinter.Text` WYSIWYG conversion, used by the template editor and the ad-hoc email compose modal); `name_utils.resolve_first_last_name()` (preferred-name-aware, casing-normalized name resolution — shared by the CSV fixture conversion today and needed again by the real Salesforce adapter later)
- `src/views/` — `AddActionView`, `ScenarioNavPanel`, `DashboardView` (default startup view — three sections: Welcome Emails, an inline "Send Email to All Students" compose area with an identical note section to a regular Action's note step, and Run Action Batch; no `AdHocEmailModal` anymore, that flow moved onto the dashboard itself per developer feedback after manual testing), `TemplateEditorModal`, `BatchRunModal` (HandBrake-style queue for running several actions in sequence — see Design decisions), `FilterRow`/`DatePickerEntry` (the filter-builder UI), and other CustomTkinter views
- `src/controllers/app_controller.py` — main app controller
- `Sample.csv` (repo root) — a real, PII-masked export of caseload fields (252 rows, real column names/shapes) the developer provided for building out realistic fake data and eventually the filter engine. `scripts/convert_sample_csv_to_fixture.py` regenerates `data/fake_students.json` from it (rerun manually whenever `Sample.csv` changes); `FakeStudentDataProvider` itself only ever reads the JSON output, not the CSV. One row was dropped (see docs/filter_engine_requirements.md) for genuine source-data corruption; `LatestCourseNote` was dropped entirely as an unneeded field.

## Working rules (follow these strictly)
1. **One focused change at a time.** Propose a single change with reasoning and a specific test to verify it, then wait for the result before moving on to the next thing.
2. **Always ask for the actual current code** before proposing changes to a file that's had prior edits — don't assume you remember its state; read the file fresh.
3. **Ask about intent (what SHOULD happen) vs. observed behavior** before proposing a fix — don't assume the cause.
4. **Preserve existing variable/field names and structures** unless they're actively misleading. Ask before renaming anything.
5. **ID-based lookups only.** Never write new code that looks up records (groups, templates, actions, or any future entity) by name — names can be renamed mid-session. Always use the record's stable ID.
6. **Name secondary issues, don't fix them.** If you spot an unrelated bug or issue while investigating something else, mention it but don't fix it until the current task is finished.
7. **Git safety net.** The developer is still learning Git. Proactively remind them when it's a good time to commit or branch (e.g. before starting a risky change, after a working milestone) — don't assume they'll remember to do it themselves. Also watch for real data files (`data/*.json`, `src/data/*.json`) picked up as "modified" from manual testing — confirm with the developer before including those in a commit; they're usually test noise, not intentional changes.

## Design decisions (settled — don't re-litigate without new input from the developer)
- **Resource access is abstracted behind interfaces** (`StudentDataProvider`, `EmailSender`, `TextSender`, `NoteWriter`), each with a local `Fake*` implementation backing everything today (`data/fake_students.json` plus in-memory records). Real Salesforce/Cadence/Outlook adapters implement the same interfaces later; nothing that calls them needs to change.
- **The app must stay platform-independent of Salesforce specifically** — the developer expects to migrate from Salesforce to a different platform ("Learner360," Salesforce-based but distinct) in roughly a year, and wants the app's logic to work "regardless of where the data comes from." This is why `Student`'s field names (`momentum`, `course_code`, etc.) are the app's *own* naming scheme rather than Salesforce's raw field/API names, and why `Student.SALESFORCE_FIELD_MAP` is treated as a Salesforce-adapter-specific detail, not something baked into `Student` itself or any app logic (filters, `ActionRunner`, views). A future Learner360 adapter would implement `StudentDataProvider` and carry its own separate field map (e.g. `LEARNER360_FIELD_MAP`) translating into the same `Student` shape — nothing else in the app should need to change. Keep this in mind for any future field mapping or data-provider work: don't let Salesforce-specific naming or assumptions leak past the adapter boundary.
- **Running an action (`ActionRunner.run`):** email and text are attempted independently per student (one failing doesn't block the other). A note step — writing the interaction note *and* updating the student's roster-visible follow-up-note field, bundled as one unit — runs only if at least one communication succeeded; otherwise it's skipped and logged, not attempted. One student's failure never stops the run — every step (success/failure/skip) is logged via `ActivityLogger` and rolled into a final succeeded/failed/skipped summary.
- **Welcome emails (`ActionRunner.run_welcome_emails`, the Dashboard's "Send Welcome Emails" button):** deliberately no separate course/student-matching logic in the app. It just runs whichever action is marked as each group's `welcome_action_id` (already existing on `Group`) through `ActionRunner`, using whatever filter the professor configured on that action themselves. This was a deliberate simplification to avoid building course-mapping or "new student" detection logic in the app.
- **Dropped:** a "new students since last checked" dashboard count. Considered, then abandoned in favor of professors using their own filter criteria (e.g. "Follow Up Note Is Empty") on their welcome action — no local "last checked" state needed. Don't resurrect this without the developer asking.
- **Filters are NOT implemented yet.** `StudentDataProvider.get_students()` and everything downstream currently act on the full roster unconditionally — filters stored on an `Action` are not applied anywhere. The developer is still working out real-world filter requirements (field-type-aware operators, AND/OR grouping, relative dates, "Is Empty"/"Is Not Empty") from how they actually use Salesforce's own filters today. Don't design or guess at a filter engine without that input — ask.
- **Outlook signatures:** the "Select Outlook Signature" dropdown has no "None" option, on purpose — signatures are required at this organization, and offering "None" invited accidentally sending an unsigned email. Signature *name* enumeration is real (`OutlookSignatureProvider`/`LocalOutlookSignatureProvider`) and used by both the Action editor and the dashboard's ad-hoc email section. The real Outlook adapter (not built yet) will still need to resolve a selected signature name to actual signature HTML (Outlook stores these under `%APPDATA%\Microsoft\Signatures\`) and append it before sending, since Outlook does not auto-apply a signature to mail sent silently via COM automation (that only happens when a compose window is actually displayed). `EmailSender.send()`'s `signature` param is currently just recorded as a label by the fake.
- **Dashboard sections are inline, not modals.** "Send Email to All Students" used to open `AdHocEmailModal` as a popup; after manual testing, moved directly onto the dashboard itself (its own scrollable section, with an identical note section to a regular Action's note step — `ActionRunner.send_ad_hoc_email()` now takes the same `note_subject`/`note_body`/`follow_up_note` params `run()` does, and applies them with the same success-gated logic). `AdHocEmailModal` was deleted as dead code once nothing referenced it anymore.
- **Batch runner (`BatchRunModal`/`ActionRunner.run_batch`)**: a HandBrake-style queue — pick any set of already-configured actions across any groups and run them in order. Deliberately *not* a new flag on `Group` (unlike `welcome_action_id`) — it generalizes `run_welcome_emails()`'s "run several actions and aggregate results" pattern to an ad-hoc, professor-chosen selection instead of a fixed one, so no new schema is needed for each new recurring cross-group need that comes up. Results are itemized per action (not just one combined total), since batch members typically target different groups/subsets. One action's unexpected failure is caught and logged, the rest of the batch continues.
- **"General" is a protected, always-present group.** It can't be renamed or deleted (`app_controller.py`'s `handle_rename_group`/`handle_delete_group`), and `_load_data()` guarantees a real, persisted "General" group exists on every startup even if `groups.json` is missing/empty — so a fresh install (no tracked `groups.json`, see below) still ends up with "General" after first launch. `ScenarioNavPanel.refresh_data()` now enforces the same guarantee at the UI layer, independent of what's actually in the `groups` list it's given.
- **Deleting a group cascades to delete its actions**, rather than reassigning them to "General" — reassigning silently to a group the professor didn't pick was considered and rejected as too likely to cause confusing, hard-to-notice state. The confirmation dialog warns how many actions will also be deleted.
- **Per-install local data files are untracked from git**, not shipped: `data/templates.json`, `src/data/actions.json`, `src/data/groups.json` are in `.gitignore` (still present on disk locally — only untracked, not deleted). Each professor creates their own groups/actions/templates in-app; a fresh install should start empty except for the "General" group, which self-heals in regardless (see above). `data/fake_students.json` is *not* untracked — it's committed seed/fixture data for `FakeStudentDataProvider`, not something a user creates.

## Known open items
- **Filter engine is built** (`filterable_fields.py` registry + `filter_engine.py` evaluation + `FilterRow`/`DatePickerEntry` UI, wired into `ActionRunner.run()`) — no longer an open item. `phone` is the one field still pending (see Design decisions history / `docs/filter_engine_requirements.md`) on outside research about a possible Cadence-ID-based texting integration.
- Real Salesforce/Cadence/Outlook adapters not built — everything runs against local fakes today, except Outlook signature *name* reading which is real (see Design decisions).
- Outlook signature resolution (name → actual HTML at send time) not implemented in the (not-yet-built) real `EmailSender` adapter — see Design decisions.
- No protection against running multiple app instances at once — plain JSON file storage with no locking, so if two windows are open simultaneously, whichever saves last silently overwrites the other's changes with no warning. Unlikely in normal single-window use, but worth keeping in mind.

## Known testing quirks (not app bugs)
- Running `pytest tests/` as one combined invocation can still produce spurious failures (e.g. a `TclError` mid-run) — multiple test files each create their own `ctk.CTk()` root in the same process, causing Tk state pollution across CustomTkinter-heavy test files. Scoped runs (e.g. `pytest tests/test_services/`, `pytest tests/test_views/`) are reliable; use those instead of chasing failures that only show up in the combined run. Note: a test failure that merely *shows up* in a combined run isn't necessarily caused by this — `test_refresh_data_updates_state_and_rerenders` was previously (mis)attributed to this quirk but turned out to be a real, deterministic logic bug reproducible in complete isolation (see Recently fixed). Confirm a failure reproduces standalone before writing it off as Tk pollution.
- Running the real app, or tests that instantiate a real `AppController`/`GroupRepository`/`ActionRepository`, writes to the actual `data/templates.json`, `src/data/actions.json`, `src/data/groups.json` — there's no tmp_path isolation for those integration paths yet. These three files are now gitignored (see Design decisions), so this no longer risks an accidental commit, but expect them to keep changing locally from test runs/manual testing.

## Recently fixed (most recent session)
- **Widened the `Student` model** from 6 fields to 68, mirroring the real caseload export the developer provided (`Sample.csv`, PII-masked). Added `Student.SALESFORCE_FIELD_MAP`, `name_utils.resolve_first_last_name()` (preferred-name-aware, casing-normalized), and `scripts/convert_sample_csv_to_fixture.py`; regenerated `data/fake_students.json` with 253 realistic students. `FakeStudentDataProvider.get_students()` now passes through every field present in a JSON record instead of naming just six explicitly.
- **Untracked per-install local data files** (`data/templates.json`, `src/data/actions.json`, `src/data/groups.json`) from git via `.gitignore` — they're hand-edited by each professor in-app, not app-shipped data, so manual-testing edits no longer show up as noisy diffs or risk being committed. Files were left untouched on disk (`git rm --cached`, not deleted).
- **Fixed three test bugs, all the same root cause** (a test built against a shape/value that was never actually implemented, rather than the code being wrong):
  - `test_add_action_view_prioritizes_action_data_group_on_edit` — used a fictional `{"metadata": {"assigned_group": ...}}` for `action_data`; corrected to the real flat `{"group_name": ...}` shape used everywhere else (`add_action_view.py`'s own save method, `app_controller.py`).
  - `test_refresh_data_updates_state_and_rerenders` — expected `refresh_data()` to guarantee "General" is always in the group dropdown; the view didn't actually do that. This was a real bug, not the test — fixed `ScenarioNavPanel.refresh_data()` to insert "General" if missing, consistent with how it's already protected/undeletable elsewhere. (Previously misattributed to Tk state pollution — see Known testing quirks.)
  - `test_action_flags_and_properties` — asserted a nonexistent `InteractionType.SINGLE_EMAIL` / `"Email to Student"`; corrected to the real `InteractionType.EMAIL` / `"Email"` used consistently everywhere else.
- **Deleting a group now cascades to delete its actions** instead of orphaning them (their `group_id` used to end up pointing at a group that no longer existed). The confirmation dialog now warns how many actions will also be deleted, each deleted action's `welcome_action_id` reference is cleared, and the actions table refreshes via the same `nav_panel.refresh_data` path used elsewhere.
- Template editor no longer hangs: swapped the pywebview-based editor (broken by a main-thread conflict with CustomTkinter's `mainloop()`) for a `CTkToplevel` modal, rebuilt as a true WYSIWYG editor (`src/services/html_richtext.py`) — bold/italic/underline/highlight/font-size/links render visually instead of as raw tags, with a raw-HTML toggle for HTML-literate professors and formatting-preserving paste from Outlook/Word.
- Template delete button was invisible (an unsupported emoji glyph rendered blank) — fixed. Deleting a template unrelated to the currently-open action now restores that action's own template selection instead of resetting to "New/Custom," and deleting the template *currently assigned* to the open action shows an explicit warning first.
- Added `ActivityLogger`, `StudentDataProvider`/`EmailSender`/`TextSender`/`NoteWriter` (+ fakes), and `ActionRunner` — see Design decisions. Wired into a real `handle_run_action`.
- Added `DashboardView` (new default startup view, nav link next to Settings) with "Send Welcome Emails" and "Send Email to All Students" (ad-hoc WYSIWYG compose via `AdHocEmailModal`).
- `NoteWriter` extended with `update_follow_up_note()` — writing a note now also updates the student's roster-visible follow-up-note field, bundled into the same step (see Design decisions).
- Removed "None" from the Outlook Signature dropdown — signatures are required at this organization; the dropdown now defaults to a real signature instead of inviting an unsigned send.
- Added `OutlookSignatureProvider`/`LocalOutlookSignatureProvider` — reads real Outlook signature names from `%APPDATA%\Microsoft\Signatures\`, replacing the hardcoded fake list in the Action editor's signature dropdown. Extended the ad-hoc "Email to All Students" modal with the same dropdown (it previously had none at all), wired through to `ActionRunner.send_ad_hoc_email`.
- Fixed the welcome-email conflict dialog never firing: `save_action()`'s conflict check read a new action's `id` as `None` (the real UUID wasn't generated until later in the same method), so the group's `welcome_action_id` was silently set to `None` instead of the action's real id. `AddActionView` now generates the real id when the form opens; the "Is Group Welcome Email" checkbox fires the confirm/replace flow live on click (reverting on cancel) instead of deferring to Save, and `populate_fields()` now correctly shows the checkbox checked on reload.
- Fixed a spurious "Switching group will discard changes" dialog appearing after every successful Save: `ScenarioNavPanel._on_group_selected()` was being reused for plain post-save/CRUD display refreshes, which also fired the controller's real group-switch confirmation guard. Split into `refresh_active_group_display()` (no callback — used by save/add/rename/delete) vs. `_on_group_selected()` (fires the callback — reserved for actual user-driven dropdown selections).
- Removed ~22 leftover `print()` debug/click-acknowledgment statements across the controller, views, and `GroupRepository`; migrated the ones with real audit value (action/group CRUD, welcome-flag changes) to `ActivityLogger` so they show up in the Live Application Log instead of only the console.
